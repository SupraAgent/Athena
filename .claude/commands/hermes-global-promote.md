Promote a local memory to the global cross-project store.

Run: `node packages/hermes/dist/cli.js global promote $ARGUMENTS`

If no arguments were provided, first run `node packages/hermes/dist/cli.js list` to show available memories, then ask the user which memory ID to promote.

Show the user the result including the global memory ID and content.
