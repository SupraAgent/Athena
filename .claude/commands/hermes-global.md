Show global cross-project memory status.

Run: `node packages/hermes/dist/cli.js global status`

Display the results to the user. If the global store is not initialized, suggest running `hermes global init`.
